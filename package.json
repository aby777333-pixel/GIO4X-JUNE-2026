{
  "name": "gio4x-june-2026",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:trader": "npm --workspace apps/trader run dev",
    "build:trader": "npm --workspace apps/trader run build",
    "lint": "npm --workspaces --if-present run lint",
    "typecheck": "npm --workspaces --if-present run typecheck"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
